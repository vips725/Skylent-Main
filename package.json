{
  "name": "skylent-global",
  "version": "1.0.0",
  "description": "Skylent Global — Build Skills. Build Careers.",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "npm --prefix backend run dev",
    "start:frontend": "node frontend/scripts/dev-server.mjs",
    "build": "npm --prefix frontend run build",
    "dev": "node scripts/dev.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
